import { View, Text } from "react-native";
import React from "react";

const SetsLoggingForm = ({
  exerciseId,
  scheduleId,
}: {
  exerciseId: string;
  scheduleId: string;
}) => {
  return (
    <View>
      <Text>SetsLoggingForm</Text>
    </View>
  );
};

export default SetsLoggingForm;
