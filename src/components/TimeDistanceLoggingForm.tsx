import { View, Text } from "react-native";
import React from "react";

const TimeDistanceLoggingForm = ({
  exerciseId,
  scheduleId,
}: {
  exerciseId: string;
  scheduleId: string;
}) => {
  return (
    <View>
      <Text>TimeDistanceLoggingForm</Text>
    </View>
  );
};

export default TimeDistanceLoggingForm;
