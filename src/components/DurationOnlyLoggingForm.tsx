import { View, Text } from "react-native";
import React from "react";

const DurationOnlyLoggingForm = ({
  exerciseId,
  scheduleId,
}: {
  exerciseId: string;
  scheduleId: string;
}) => {
  return (
    <View>
      <Text>DurationOnlyLoggingForm</Text>
    </View>
  );
};

export default DurationOnlyLoggingForm;
