import { View, Text, SafeAreaView } from "react-native";
import CustomText from "@/src/components/CustomText";
import React from "react";

const Chats = () => {
  return (
    <SafeAreaView>
      <CustomText>Chats</CustomText>
    </SafeAreaView>
  );
};

export default Chats;
